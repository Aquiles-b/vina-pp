<A fazer> -m Move o membro para trás do target existente passado. Deve ocorrer
na seção de dados.

<A fazer> -h Help para os comandos.

<A fazer> lidar com diretorios.

<Feito> -i Opção de inserir mais membros no archive. Se um membro já existe deve
ser substituído e colocado inseridos respeitando a ordem da linha de comando,
ao final do archive;
<Feito> -a É igual -i porém só substitui se for mais recente.
<Feito> -x Extração dos membros indicados. Caso nenhum seja indicado extrair 
todos.
<Feito> -r Remove membros indicados.
<Feito> -c Lista o conteúdo do archive em ordem com todas as propriedade do 
membro e sua ordem no arquivo (igual comando tvf do tar).

OBS:
    Caminhos sempre relativos.
    Deve existir uma área diretório(cabeçalho) com todas as informações dos 
    membros 
    O conteúdo dos membros devem ser manipulados em blocos de 1024 bytes.

Propriedades dos membros:
    nome (sem espaços), 
    UID (user ID),
    permissões,
    tamanho,
    data de modificação,
    ordem no arquivo (Ordem de inserção, pode ser alterada pela opção -m)
    localização.

tar tvf:
    drwx------ UID/bcc         0 2023-05-31 19:25 2022/
    -rw------- UID/bcc       163 2023-05-31 17:48 2022/makefile
    -rw------- UID/bcc      3392 2023-05-31 19:24 2022/main.c
